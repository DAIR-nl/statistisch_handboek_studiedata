```{r}

library(downloadthis)

download_file(
  path = "Datasets_zipbestand",
  output_name = "Zippie",
  button_label = "Download Zippie",
  button_type = "danger",
  has_icon = TRUE,
  icon = "fa fa-save",
  self_contained = FALSE
)
```